# IntranetUi

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 11.2.2.

## First of all - firebase config

Rename the files `src/environments/dist.firebase.prod.ts` to `src/environments/firebase.prod.ts` and `src/environments/dist.firebase.ts` to `src/environments/firebase.ts` with the firebase config.
